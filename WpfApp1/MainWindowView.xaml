<Window x:Class="WpfApp1.MainWindow.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:WpfApp1.ApplicationLogic;assembly=Application"
        xmlns:views="clr-namespace:WpfApp1.View;assembly=View"
        mc:Ignorable="d"
        Height="490" Width="800" 
        WindowStyle="None" ResizeMode="NoResize"
        BorderBrush="Black" BorderThickness="2" 
        MouseDown="Window_MouseDown">
    <Window.Resources>
        <DataTemplate DataType = "{x:Type viewModels:PanelViewModel}">
            <views:PanelView></views:PanelView>
        </DataTemplate>
        <DataTemplate DataType = "{x:Type viewModels:TestViewModel}">
            <views:TestView></views:TestView>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="A">
                <ContentControl Content="{Binding PanelViewModel}"></ContentControl>
            </TabItem>
            <TabItem Header="B">
                <ContentControl Content="{Binding TestViewModel}"></ContentControl>
            </TabItem>
        </TabControl>
    </Grid>
    
</Window>
